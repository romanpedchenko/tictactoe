import { RepeatRowDirective } from './repeat-row.directive';

describe('RepeatRowDirective', () => {
  it('should create an instance', () => {
    const directive = new RepeatRowDirective();
    expect(directive).toBeTruthy();
  });
});
